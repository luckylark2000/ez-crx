# 相关模块说明

## src/popup

是浏览器插件的入口文件，里面放置了插件的UI界面。

## src/content

插件的脚本文件，里面放置了插件的逻辑。

## src/sidepanel

插件的侧边栏，里面放置了插件的UI界面。

右键插件的图标，点击`打开侧边栏`或者`open side panel`，就会打开侧边栏。

这个界面是单独的一个页面，和插件的主界面是两个页面。

如果两个内容不想相同就写不同的内容就行。

如果想基本相同的话，就可以组件化，然后分别引用即可。
